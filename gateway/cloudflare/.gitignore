/target
/build
/dist
/.wrangler
node_modules/

# Local deployment configuration — never commit real Network values.
# (.dev.vars.example is the committed template with empty values.)
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
